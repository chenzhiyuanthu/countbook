import SwiftUI

// Stubs so the shell compiles while the feature screens are being written.
// Each is replaced by its own file; delete this one when the last lands.

struct TodayScreen: View {
    var body: some View { Color.clear }
}

struct LedgerScreen: View {
    var body: some View { Color.clear }
}

struct ReportScreen: View {
    var body: some View { Color.clear }
}

struct WantsScreen: View {
    var body: some View { Color.clear }
}

struct SettingsScreen: View {
    var body: some View { Color.clear }
}

struct CaptureScreen: View {
    let onClose: () -> Void
    var body: some View { Color.clear }
}

struct ReckoningScreen: View {
    let onClose: () -> Void
    var body: some View { Color.clear }
}
